import React from 'react';
import ReactDOM from 'react-dom/client';
import App from "./App"

const root = ReactDOM.createRoot(document.getElementById('root'));

// const h1Style = {
//     color: "red"
// }

// const name = "Bob"
// const message = `Hello my name is ${name} and current time is ${Date.now()}`

root.render(
    <App />
);
